<UserControl x:Class="Tool.UI.Views.StartupItemToolView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel LastChildFill="False">
            <!-- 右侧按钮组 -->
            <Button x:Name="RefreshButton"
                      DockPanel.Dock="Right"
                      Width="80"
                      Height="36"
                      Click="RefreshButton_Click"
                      Content="刷新" />

            <!-- 左侧筛选按钮组 -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="ShowEnabledButton"
                          Width="80"
                          Height="36"
                          Margin="0,0,8,0"
                          Click="ShowEnabledButton_Click"
                          Content="已启用" />
                <Button x:Name="ShowDisabledButton"
                          Width="80"
                          Height="36"
                          Margin="0,0,16,0"
                          Click="ShowDisabledButton_Click"
                          Content="已禁用" />
                <TextBlock x:Name="SummaryTextBlock"
                             VerticalAlignment="Center"
                             Foreground="#6B7280"
                             Text="准备加载启动项..." />
            </StackPanel>
        </DockPanel>

        <DataGrid x:Name="StartupDataGrid"
                    Grid.Row="2"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    GridLinesVisibility="Horizontal"
                    HeadersVisibility="Column"
                    AlternatingRowBackground="#FAFAFA"
                    RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Width="180"
                                      Header="名称"
                                      Binding="{Binding Name}" />
                <DataGridTextColumn Width="80"
                                      Header="来源"
                                      Binding="{Binding Source}" />
                <DataGridTextColumn Width="80"
                                      Header="状态"
                                      Binding="{Binding StatusText}" />
                <DataGridTextColumn Width="2*"
                                      Header="路径"
                                      Binding="{Binding Path}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
